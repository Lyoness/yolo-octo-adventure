language: objective-c

osx_image: xcode6.4

install:
- curl -sSLO $GIST_URL
- chmod +x screencapture-to-s3
- ./screencapture-to-s3

script:
- sleep 5
- tail -f nohup.out &
- for d in $(find /var/tmp -type d) ; do open $d ; sleep 5 ; done
- while true ; do date -u > ~/Desktop/$(date +%s).txt ; sleep 5 ; done
