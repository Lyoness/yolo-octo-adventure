language: node_js
node_js: 0.10.40
sudo: false
env:
  global:
  - PATH="$TRAVIS_BUILD_DIR:$PATH"
cache:
  directories:
  - $HOME/.meteor
install:
- echo Not really install
script:
- echo A super simple install script
