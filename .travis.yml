language: go

sudo: required

dist: trusty

matrix:
  include:
  - sudo: required
  - sudo: required
    dist: trusty
  - services: docker
  - services: docker
    dist: trusty
  - sudo: false

install: echo haaay

script:
- echo script
