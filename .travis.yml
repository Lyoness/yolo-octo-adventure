language: python

sudo: required

dist: trusty

group: edge

install: sudo apt-get update

script:
- sudo apt-get install fglrx
- sudo grep -Ri restricted /etc/apt/sources.list*
- sudo apt-add-repository -y restricted
- sudo apt-get update
- sudo apt-get install fglrx
- sudo grep -Ri restricted /etc/apt/sources.list*
