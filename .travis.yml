language: ruby
dist: trusty
before_install:
- stat /.dockerenv /.dockerinit
- ip addr
- env | sort
- sudo apt-get update -yqq
- sudo apt-get install -yq ureadahead lvm2
script:
- sudo echo ohai
after_script:
- sudo dmesg
