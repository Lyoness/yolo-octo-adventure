language: php
install: echo install
script:
- find ~/ -name python -type f || true
- python -V || true
- python3 -V || true

matrix:
  include:
  - sudo: false
  - sudo: required
  - sudo: 9000
    language: generic
