language: ruby
sudo: false
cache:
  bundler: true
notifications:
  email: false
addons:
  apt_sources:
  - chef-stable-precise
  - deadsnakes-precise
  - unknown-source
  apt_packages:
  - chefdk
before_script:
- gem install rack
- rackup --port 8000 &
