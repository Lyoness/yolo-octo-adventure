language: go

sudo: false

addons:
  apt:
    sources:
    - precise-pgdg-9.5
    packages:
    - postgresql-9.5
  postgresql: 9.5

before_script:
- psql --version
- ps auxwww | grep -iE 'pg_|postg'
- psql -p 5433 -c 'select version();'
