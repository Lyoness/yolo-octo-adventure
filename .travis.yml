language: go

sudo: false

addons:
  apt:
    sources:
    - precise-pgdg-9.5
    packages:
    - libpq5
    - postgresql-client-9.5
    - postgresql-9.5
  postgresql: 9.5

before_script:
- psql --version
- psql -c 'select version();'
