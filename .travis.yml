language: objective-c

osx_image: xcode6.4

env:
  matrix:
  - RUBY_VERSION=1.9.3
  - RUBY_VERSION=2.1.2
  - RUBY_VERSION=2.1.7
  - RUBY_VERSION=2.2.1
  - RUBY_VERSION=2.2.2

install: echo install

script:
- rvm use $RUBY_VERSION --install --binary --fuzzy
