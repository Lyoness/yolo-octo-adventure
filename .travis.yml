language: bash
sudo: false
install: echo get real
script: echo whatebbs
notifications:
  email: false
  irc:
    template:
    - "%{repository}/%{branch}/%{build_number}: %{message} -- %{build_url}"
    channels:
    - irc.freenode.net#meatballhat
