language: generic

sudo: required

group: edge

services:
- docker

install:
- rvm list
- rvm use default
- bundle install --jobs=3 --retry=3

script:
- echo script whee
- uname -a
- sudo apparmor_status || true
